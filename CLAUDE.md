# Project Instructions

## Git

- **Never commit or push unless explicitly asked.** Make code changes and stop. Do not run `git add`, `git commit`, or `git push` unless the user says so (e.g. "commit this", "go ahead and commit", "commit and push").
- Commit author must always be: `Abdullahi Mahmood <mahmoodsaadiq@gmail.com>`

## Style

- Tailwind CSS only — no shadcn/ui, no new component libraries.
- Emerald/zinc colour palette throughout.
